import React from 'react'
import Layout from '../components/layout/layout'

const Newest = (props) => {
    return (
        <Layout>
            <h1>Newest page</h1>
        </Layout>
    )
}

export default Newest